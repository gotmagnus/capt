import { Bodoni_Moda } from "next/font/google";

// Didone letterforms are the hand of stock certificates and banknote engraving.
export const bodoni = Bodoni_Moda({ subsets: ["latin"], axes: ["opsz"], variable: "--font-bodoni", display: "swap" });
